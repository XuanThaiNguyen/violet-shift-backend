{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["dist", "node_modules"],
  "exec": "ts-node --transpile-only src/consumer.ts",
  "signal": "SIGTERM",
  "env": {
    "NODE_ENV": "development"
  }
}
